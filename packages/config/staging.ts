import type ENV from './types';

const CONFIG: ENV = {
  ENV: 'staging',
  API_URL: 'staging',
  LANDERS_API_URL: 'https://staging.landers.ph',

  LANDERS_CONTAINER_URL: 'http://localhost:9001',
};

export default CONFIG;
