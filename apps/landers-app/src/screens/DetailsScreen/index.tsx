import React from 'react';
import { styles } from './styles';
import { Text, View } from 'react-native';

const HomeScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.label}>Details Screen</Text>
    </View>
  );
};

export default HomeScreen;
